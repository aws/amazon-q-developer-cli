# work/ — work items

The planner writes one file per work item here. See `../orchestrator.md`
for how they are produced and `../work-item.template.md` for the shape.

This file exists to keep the directory in git. Delete it once real work items
are being produced, or add `_manifest.md` and the numbered work-item files
alongside it.
