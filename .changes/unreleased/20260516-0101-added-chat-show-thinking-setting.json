{
  "type": "added",
  "description": "Added `chat.showThinking` setting to display the agent's reasoning (\"Thinking\") blocks in chat output. Enabled by default — disable via `/settings` → Display → Show thinking toggle."
}
