{
  "type": "added",
  "description": "Added `chat.showThinking` setting to display the agent's reasoning (\"Thinking\") blocks in chat output. Disabled by default — enable with `kiro-cli settings chat.showThinking true`."
}
