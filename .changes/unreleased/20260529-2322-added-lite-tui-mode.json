{
  "type": "added",
  "description": "Lite UI mode: a lightweight, scrollback-friendly chat experience. Switch to it with `/lite` mid-session, or set it as your default via `/settings → display` (Default UI row). First-time users get prompted to pick at launch. Tune output density with `/verbosity`."
}
