// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum DocumentCitationLocation {
    #[allow(missing_docs)] // documentation missing in model
    DocumentChar(crate::types::DocumentCitationSpan),
    #[allow(missing_docs)] // documentation missing in model
    DocumentChunk(crate::types::DocumentCitationSpan),
    #[allow(missing_docs)] // documentation missing in model
    DocumentPage(crate::types::DocumentCitationSpan),
    #[allow(missing_docs)] // documentation missing in model
    SearchResult(crate::types::DocumentCitationSearchResultSpan),
    #[allow(missing_docs)] // documentation missing in model
    Web(crate::types::DocumentCitationWebLocation),
    /// The `Unknown` variant represents cases where new union variant was received. Consider
    /// upgrading the SDK to the latest available version. An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has
    /// not been updated. To investigate this, consider turning on debug logging to print the
    /// raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl DocumentCitationLocation {
    /// Tries to convert the enum instance into
    /// [`DocumentChar`](crate::types::DocumentCitationLocation::DocumentChar), extracting the inner
    /// [`DocumentCitationSpan`](crate::types::DocumentCitationSpan). Returns `Err(&Self)` if it
    /// can't be converted.
    pub fn as_document_char(&self) -> ::std::result::Result<&crate::types::DocumentCitationSpan, &Self> {
        if let DocumentCitationLocation::DocumentChar(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }

    /// Returns true if this is a
    /// [`DocumentChar`](crate::types::DocumentCitationLocation::DocumentChar).
    pub fn is_document_char(&self) -> bool {
        self.as_document_char().is_ok()
    }

    /// Tries to convert the enum instance into
    /// [`DocumentChunk`](crate::types::DocumentCitationLocation::DocumentChunk), extracting the
    /// inner [`DocumentCitationSpan`](crate::types::DocumentCitationSpan). Returns `Err(&Self)`
    /// if it can't be converted.
    pub fn as_document_chunk(&self) -> ::std::result::Result<&crate::types::DocumentCitationSpan, &Self> {
        if let DocumentCitationLocation::DocumentChunk(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }

    /// Returns true if this is a
    /// [`DocumentChunk`](crate::types::DocumentCitationLocation::DocumentChunk).
    pub fn is_document_chunk(&self) -> bool {
        self.as_document_chunk().is_ok()
    }

    /// Tries to convert the enum instance into
    /// [`DocumentPage`](crate::types::DocumentCitationLocation::DocumentPage), extracting the inner
    /// [`DocumentCitationSpan`](crate::types::DocumentCitationSpan). Returns `Err(&Self)` if it
    /// can't be converted.
    pub fn as_document_page(&self) -> ::std::result::Result<&crate::types::DocumentCitationSpan, &Self> {
        if let DocumentCitationLocation::DocumentPage(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }

    /// Returns true if this is a
    /// [`DocumentPage`](crate::types::DocumentCitationLocation::DocumentPage).
    pub fn is_document_page(&self) -> bool {
        self.as_document_page().is_ok()
    }

    /// Tries to convert the enum instance into
    /// [`SearchResult`](crate::types::DocumentCitationLocation::SearchResult), extracting the inner
    /// [`DocumentCitationSearchResultSpan`](crate::types::DocumentCitationSearchResultSpan).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_search_result(&self) -> ::std::result::Result<&crate::types::DocumentCitationSearchResultSpan, &Self> {
        if let DocumentCitationLocation::SearchResult(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }

    /// Returns true if this is a
    /// [`SearchResult`](crate::types::DocumentCitationLocation::SearchResult).
    pub fn is_search_result(&self) -> bool {
        self.as_search_result().is_ok()
    }

    /// Tries to convert the enum instance into
    /// [`Web`](crate::types::DocumentCitationLocation::Web), extracting the inner
    /// [`DocumentCitationWebLocation`](crate::types::DocumentCitationWebLocation).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_web(&self) -> ::std::result::Result<&crate::types::DocumentCitationWebLocation, &Self> {
        if let DocumentCitationLocation::Web(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }

    /// Returns true if this is a [`Web`](crate::types::DocumentCitationLocation::Web).
    pub fn is_web(&self) -> bool {
        self.as_web().is_ok()
    }

    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
