/**
 * Trails a finished task so its state reaches a screen reader. Strikethrough,
 * colour, and the status glyph are all attributes or symbols a reader skips,
 * and every surface showing tasks has to say the same word for it.
 */
export const TASK_DONE_MARKER = '[done]';
