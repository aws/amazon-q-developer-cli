# Notes

- [x] Bundle a multi-language workspace
- [x] Nest folders to show tree indentation
- [ ] Rotate themes with `Tab` while this file is open
- [ ] Edit a line, then save with `Ctrl+S`

Markdown highlights **bold**, _italic_, `inline code`, and
[links](https://example.com).

> Tip: the dirty marker (●) in the statusline appears the moment the buffer
> differs from what's on disk.
